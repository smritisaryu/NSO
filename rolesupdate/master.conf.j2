vrrp_instance VI_1{
  state MASTER # Set to MASTER for the primary server and BACKUP for the secondary server
  interface eth0 # Replace with the network interface to be used
  virtual_router_id 51
  priority 100 # Set a higher priority for the primary server and a lower priority for the secondary server
  advert_int 1

 virtual_ipaddress{
        {{ vip_addr }}}
}
